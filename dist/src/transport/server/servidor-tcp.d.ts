import { CustomTransportStrategy, Server } from '@nestjs/microservices';
import { IServidorTCPConfig, ISocket } from '../../contracts';
import * as Net from 'node:net';
export declare class ServidorTcp extends Server implements CustomTransportStrategy {
    private static readonly conexoesTcp;
    private readonly configuracao;
    private readonly conexoesAtivas;
    private readonly mensagensIncompletasPorSocket;
    private readonly processamentosPorSocket;
    private readonly separarMsgs;
    private encerrandoServidor;
    private servidor?;
    constructor(configuracao: IServidorTCPConfig);
    listen(callback: () => void): void;
    on(evento: string, callback: Function): void;
    unwrap<T = Net.Server>(): T;
    static obterConexao(imei: string): ISocket | null;
    close(): void;
    private configurarConexao;
    private mensagem;
    private enfileirarProcessamento;
    private processarDadosRecebidos;
    private processarMensagemComTratamento;
    private processarMensagem;
    private criarContexto;
    private registrarConsumidorAusente;
    private registrarConsumidorIncompativel;
    private registrarErroProcessamento;
    private obterDescricaoErro;
    private timeOut;
    private monitorarConexaoFechada;
    private clienteDesconectou;
    private removerEstadoDaConexao;
    private emitirEventoConexaoFechada;
    private conexaoErro;
    private salvarConexao;
    private qtdDispositivosConectados;
    separarMensagens(mensagem: Buffer): string[];
    private separarMensagensComBruto;
    private separarMensagensMantendoEstadoDoSocket;
    private atualizarMensagemIncompleta;
    private descartarMensagemIncompleta;
}
